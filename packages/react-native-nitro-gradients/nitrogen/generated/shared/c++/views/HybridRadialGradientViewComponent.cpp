///
/// HybridRadialGradientViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridRadialGradientViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::gradient::views {

  extern const char HybridRadialGradientViewComponentName[] = "RadialGradientView";

  HybridRadialGradientViewProps::HybridRadialGradientViewProps(const react::PropsParserContext& context,
                                                               const HybridRadialGradientViewProps& sourceProps,
                                                               const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    colors([&]() -> CachedProp<std::vector<double>> {
      try {
        const react::RawValue* rawValue = rawProps.at("colors", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.colors;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::vector<double>>::fromRawValue(*runtime, value, sourceProps.colors);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RadialGradientView.colors: ") + exc.what());
      }
    }()),
    positions([&]() -> CachedProp<std::optional<std::vector<double>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("positions", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.positions;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::vector<double>>>::fromRawValue(*runtime, value, sourceProps.positions);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RadialGradientView.positions: ") + exc.what());
      }
    }()),
    center([&]() -> CachedProp<std::optional<Vector>> {
      try {
        const react::RawValue* rawValue = rawProps.at("center", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.center;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<Vector>>::fromRawValue(*runtime, value, sourceProps.center);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RadialGradientView.center: ") + exc.what());
      }
    }()),
    radius([&]() -> CachedProp<std::optional<std::variant<std::string, double>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("radius", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.radius;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::variant<std::string, double>>>::fromRawValue(*runtime, value, sourceProps.radius);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RadialGradientView.radius: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridRadialGradientViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridRadialGradientViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("RadialGradientView.hybridRef: ") + exc.what());
      }
    }()) { }

  HybridRadialGradientViewProps::HybridRadialGradientViewProps(const HybridRadialGradientViewProps& other):
    react::ViewProps(),
    colors(other.colors),
    positions(other.positions),
    center(other.center),
    radius(other.radius),
    hybridRef(other.hybridRef) { }

  bool HybridRadialGradientViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("colors"): return true;
      case hashString("positions"): return true;
      case hashString("center"): return true;
      case hashString("radius"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridRadialGradientViewComponentDescriptor::HybridRadialGradientViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridRadialGradientViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                              const std::shared_ptr<const react::Props>& props,
                                                                                              react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridRadialGradientViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridRadialGradientViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = dynamic_cast<HybridRadialGradientViewShadowNode&>(shadowNode);
    const HybridRadialGradientViewProps& props = concreteShadowNode.getConcreteProps();
    HybridRadialGradientViewState state;
    state.setProps(props);
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::gradient::views
