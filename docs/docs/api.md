---
sidebar_position: 2
---

# API Index

This page summarizes exports and links to detailed docs.

## Components

- `LinearGradient` / `AnimatedLinearGradient` → see "LinearGradient" page
- `RadialGradient` / `AnimatedRadialGradient` → see "RadialGradient" page
- `SweepGradient` / `AnimatedSweepGradient` → see "SweepGradient" page

## Types

- `Vector`, `PercentString`, `RadiusValue`, `ColorValue` → see "Type Definitions"

## Reanimated guides
- Reanimated integration → see "Reanimated Integration"
